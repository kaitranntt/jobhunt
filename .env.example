# Supabase Configuration
# Get these values from: https://supabase.com/dashboard/project/[your-project]/settings/api

# Supabase Project URL
# Format: https://[project-ref].supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Publishable Default Key (NEW API - client-side safe)
# Starts with "sb_publishable_"
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_your-publishable-key

# Supabase Secret Key (server-side only - NEVER expose in client code)
# Use only in API routes, server components, or backend services
# Starts with "sb_secret_"
SUPABASE_SECRET_KEY=sb_secret_your-secret-key

# Database Password (for direct database connections if needed)
# Used for connection pooling or direct PostgreSQL access
SUPABASE_DB_PASSWORD=your-database-password
